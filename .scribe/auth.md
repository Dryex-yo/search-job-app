# Authenticating requests

To authenticate requests, include an **`Authorization`** header with the value **`"Bearer Bearer YOUR_API_TOKEN"`**.

All authenticated endpoints are marked with a `requires authentication` badge in the documentation below.

<b>Authentication:</b> Use Laravel Sanctum tokens. Login to your account and generate an API token from your dashboard to access the API.
